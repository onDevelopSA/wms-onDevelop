<?xml version="1.0" encoding="utf-8"?>
<odoo>
  <record id="tree_product_location_quant" model="ir.ui.view">
    <field name="name">tree.product.location.quant</field>
    <field name="model">product.location.quant</field>
    <field name="arch" type="xml">
      <tree string='Product Location Quant'>
	<field name="location_id" string="Location"/>
	<field name="product_id" string="Product"/>
	<field name="minimal_quant" string="Minimal Quantity"/>
      </tree>
    </field>
  </record>

  <record id="form_product_location_quant_view" model="ir.ui.view">
    <field name="name">form.product.location.quant.view</field>
    <field name="model">product.location.quant</field>
    <field name="arch" type="xml">
      <form>
        <sheet>
	  <separator string="Minimal Product Quantity by Location"/>
	  <group col="2">
	    <field name="location_id" string="Location"/>
	  </group>
	  <group col="4">
	    <field name="product_id" string="Product"/>
	    <field name="minimal_quant" string="Minimal Quant"/>
	  </group>
	  
	  <!-- <notebook> -->
	  <!--   <page id="remote_printer" string="Remote Printer" -->
	  <!-- 	  attrs="{'invisible': [('local_printer', '=', True)]}"> -->
	  <!--     <group string='Fiscal Numbers' col="2"> -->
	  <!-- 	<field name="url_fiscal_number" string="Fiscal Number URL" -->
	  <!-- 	       help="Used for get the fiscal number in the new invoices."/> -->
	  <!-- 	  <field name="url_credit_note" string="Credit Note URL" -->
	  <!-- 		 help="Used for get the fiscal number in the client credit note."/> -->
	  <!-- 	  <field name="url_reprint_invoice" string="Reprint Invoice URL" -->
	  <!-- 		 help="Used for reprint the invoice."/> -->
	  <!-- 	</group> -->
	  <!-- 	<group string='Reports XZ' col="2"> -->
	  <!-- 	  <field name="url_xz_report" string="X Report URL" -->
	  <!-- 		 help="Used for get the X and Z reports."/> -->
	  <!-- 	</group> -->
	  <!--     </page> -->
	  <!--     <page id="local_server_printer" string="Local Printer"> -->
	  <!-- 	<group string='Server Address' col="2"> -->
	  <!-- 	  <field name="server_address" string="Server Address" -->
	  <!-- 		 help='Specific address where the "csv" file will\ -->
	  <!-- 		       be placed in the server.'/> -->
	  <!-- 	</group> -->
	  <!--     </page> -->
	  <!--   </notebook> -->
	</sheet>
	<div class="oe_chatter">
          <field name="message_follower_ids" widget="mail_followers"/>
          <field name="message_ids" widget="mail_thread"/>
        </div>
	</form>
      </field>
    </record>
</odoo>
